apiVersion: v1
kind: Service
metadata:
  labels:
    app: webtest
  name: webtest
spec:
  ports:
  - name: http
    port: 9113
    protocol: TCP
    targetPort: http
  selector:
    app: webtest
  type: LoadBalancer
status:
  loadBalancer: {}