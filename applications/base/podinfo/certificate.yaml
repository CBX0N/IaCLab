---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-webtest
spec:
  secretName: cert-webtest
  issuerRef:
    name: cert-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - 'webtest.cbxon.co.uk'